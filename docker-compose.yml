version: '3.8'

services:
  bot:
    build: .
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}   # ← Referência à variável do Portainer
    volumes:
      - ./data:/app/data
    restart: unless-stopped